// Jest setup file
// Add global test utilities and mocks here

beforeAll(async () => {
  // Setup test database connection
});

afterAll(async () => {
  // Close database connections
});

beforeEach(() => {
  // Reset mocks
});
